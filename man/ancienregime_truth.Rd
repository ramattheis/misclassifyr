% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_ancienregime_truth.R
\docType{data}
\name{ancienregime_truth}
\alias{ancienregime_truth}
\title{The generating truth behind the Ancien Regime datasets}
\format{
A named list:
\describe{
\item{occupations}{The six occupational classes, lowest to highest.}
\item{provinces}{The twelve provinces of the \code{ancienregime} world.}
\item{parish_register}{A data frame mapping the integer parish codes
of \link{ancienregime_parishes} to parish names, provinces, and 1750
population shares.}
\item{parishes}{The parish world's parameters: \code{alpha} (the two
false-link rates), \code{T_decade} (the 240 x 240 decade-long
parish-to-parish transition operator), and the latent parish
margins \code{margin_1750}, \code{margin_1770}, \code{margin_1780}.}
\item{vingtieme_flows}{A flow table \verb{(j, l, t, n)} built from the
(fictional) vingtieme of 1780, which asked every household where
it resided in 1770: the decade operator as an analyst would
observe it, sampling noise included. Pass \verb{(j, l, t)} as the \code{T2}
argument of \code{\link[=misclassifyr_rl_em]{misclassifyr_rl_em()}}.}
\item{lineages}{The lineage world's parameters: \code{alpha} (the three
marginal false-link rates of the son's links), \code{s} and \code{pi_shared}
and \code{b} (the dependence structure: \code{alpha = pi_shared + (1 - pi_shared) * b}), \code{alpha_f} (the father's link), \code{mu} (the
clerk's miscoding share), \code{Pi} (the father-to-son transmission
matrix -- the estimand), \code{T_occ} (the decade operator on
occupations), \code{occupation_slab} (the latent slab failed links draw
from), \code{father_margin}, and \code{kernel} (the clerk's adjacent-rung
confusion kernel).}
}
}
\source{
Generated alongside the datasets by
\code{data-raw/make_ancienregime_extras.R}.
}
\usage{
data(ancienregime_truth)
}
\description{
The parameters used to generate \link{ancienregime_parishes} and
\link{ancienregime_lineages}, together with the parish register and the
auxiliary tables an analyst would bring to the linked data. Shipping
the truth keeps the worked examples honest: every estimate in
\code{vignette("tour-ancien-regime")} is compared against the value that
generated the data, and the package's tests assert the comparison.
Real data never comes with this object; that is the point of
synthetic data.
}
\examples{
data(ancienregime_truth)
ancienregime_truth$lineages$alpha     # the rates the vignette recovers
head(ancienregime_truth$parish_register)
}
\seealso{
\link{ancienregime_parishes}, \link{ancienregime_lineages}, and
\code{vignette("tour-ancien-regime")}.
}
\keyword{datasets}
